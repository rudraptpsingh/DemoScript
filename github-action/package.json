{
  "name": "demoscript-action",
  "version": "1.0.0",
  "description": "DemoScript GitHub Action — auto-render demo on push",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build src/main.ts -o dist --license licenses.txt",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/artifact": "^2.1.11",
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20"
  }
}
